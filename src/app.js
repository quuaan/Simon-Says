

var pattern = [];
  var usedPattern = [];
  var level = 0;
  var gameCheck = false;

  function addPattern() {
    var pValue = randNum(1, 4);

    // add pattern to array
    pattern.push(pValue);
  }

